{
  "name": "nft-manager-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for NFT Manager contract events",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nft-minted",
    "create-local": "graph create --node http://localhost:8020/ nft-manager-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ nft-manager-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nft-manager-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.66.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}




